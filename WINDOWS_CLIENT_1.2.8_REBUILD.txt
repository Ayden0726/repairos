WorkshopOS Windows client 1.2.8 - rebuild on Windows
====================================================

Banner must show: Client 1.2.8
Build script banner must show: WorkshopOS client build script v6

XamlCompiler fix (1.2.8 republish):
- QuoteBuilder NumberBox fields use double (not decimal) — WinUI x:Bind
  TwoWay decimal→NumberBox.Value caused MSB3073 / XamlCompiler exit 1
- Removed x:Name inside line-item DataTemplate; summary binds string helpers

From repo root — use Developer PowerShell for VS 2022 (so MSBuild is found):

  git pull origin main

  powershell -ExecutionPolicy Bypass -File .\packaging\build-client.ps1 -Configuration Release -Version 1.2.8 -SkipInstaller

Or wipe + verify + build + launch:

  powershell -ExecutionPolicy Bypass -File .\packaging\rebuild-client.ps1

On failure, read:
  packaging\out\logs\publish-last.log

Outputs:
  packaging\dist\WorkshopOS-Client-win-x64-v1.2.8.zip
  packaging\dist\WorkshopOS-Setup-1.2.8.exe  (if Inno Setup 6 installed)

Optional agent overlay if GitHub looks stale:
  http://127.0.0.1:28765/repairos-github-sync.zip
  Set $UseAgentOverlay = $true in packaging\rebuild-client.ps1
