<Page x:Class="WorkshopOS.Client.Views.BackupsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Padding="24" RowSpacing="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid>
      <TextBlock Text="Backups" FontSize="22" FontWeight="SemiBold" />
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
        <Button Content="Refresh" Command="{x:Bind ViewModel.RefreshCommand}" />
        <Button Content="Create backup" Command="{x:Bind ViewModel.CreateCommand}" Style="{StaticResource AccentButtonStyle}" />
      </StackPanel>
    </Grid>
    <StackPanel Grid.Row="1" Spacing="4">
      <TextBlock Text="{x:Bind ViewModel.Status, Mode=OneWay}" Opacity="0.7" TextWrapping="Wrap" />
      <TextBlock Text="{x:Bind ViewModel.Error, Mode=OneWay}" Foreground="DarkRed" />
    </StackPanel>
    <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Lines, Mode=OneWay}" />
  </Grid>
</Page>
