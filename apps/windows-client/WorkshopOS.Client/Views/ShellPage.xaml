<Page
    x:Class="WorkshopOS.Client.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:contracts="using:WorkshopOS.Contracts.Common">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="240" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Padding="16" Spacing="2">
          <TextBlock Text="{x:Bind ViewModel.ProductName, Mode=OneWay}" FontWeight="SemiBold" FontSize="16" />
          <TextBlock Text="{x:Bind ViewModel.BusinessName, Mode=OneWay}" Opacity="0.7" FontSize="12" />
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Modules, Mode=OneWay}"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemClick="Module_ItemClick">
          <ListView.ItemTemplate>
            <DataTemplate x:DataType="contracts:ModuleDto">
              <StackPanel Padding="4,6" Spacing="2">
                <TextBlock Text="{x:Bind Title}" />
                <TextBlock FontSize="11" Opacity="0.55" Text="{x:Bind Section}" />
              </StackPanel>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="2" Padding="12" Spacing="8">
          <TextBlock Text="{x:Bind ViewModel.UserDisplay, Mode=OneWay}" FontSize="12" />
          <Button Content="Sign out" Command="{x:Bind ViewModel.LogoutCommand}" HorizontalAlignment="Stretch" />
        </StackPanel>
      </Grid>
    </Border>
    <Grid Grid.Column="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Grid Padding="16,12" ColumnSpacing="12">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox PlaceholderText="Search tickets, customers, serials… (Ctrl+K)"
                 Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay}"
                 KeyDown="OnSearchKeyDown" />
        <Button Grid.Column="1" Content="Search" Command="{x:Bind ViewModel.SearchCommand}" />
        <Button Grid.Column="2" Content="Alerts" Click="Notifications_Click" ToolTipService.ToolTip="Notifications" />
      </Grid>
      <TextBlock Grid.Row="1" Margin="16,0,16,8" Text="{x:Bind ViewModel.SearchStatus, Mode=OneWay}" FontSize="12" Opacity="0.7" TextWrapping="Wrap" />
      <Frame x:Name="ContentFrame" Grid.Row="2" />
    </Grid>
  </Grid>
</Page>
